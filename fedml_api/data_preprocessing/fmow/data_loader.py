def load_all_data_fmow(batch_size, current_train_iteration, num_client, data_dir):
    return None
    

def load_partition_data_fmow(batch_size, current_train_iteration,
                            num_client, retrain_data, data_dir):
    return None
